# secrets — never commit
*.env
!secrets.example.env
.credentials.yaml
*.bak.*
*.orig

# local scratch
node_modules/
.DS_Store
.last-enroll.json

# build output (the Windows payload is ~300 MB; it lives on a release, not in git)
dist/
